namespace Dungeon.Magic
{
    /// <summary>Lifetime / motion rules for hero magic VFX (no damage yet).</summary>
    public enum MagicSpellKind
    {
        RayBurst,
        ProjectileOrb,
        ProjectileFast,
        AttachedShield
    }
}
