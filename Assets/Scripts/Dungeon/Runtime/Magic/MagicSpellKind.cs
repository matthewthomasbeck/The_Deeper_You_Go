namespace Dungeon.Magic
{
    /// <summary>Lifetime / motion rules for hero magic VFX; damage is applied in projectile / ray behaviours.</summary>
    public enum MagicSpellKind
    {
        RayBurst,
        ProjectileOrb,
        ProjectileFast,
        AttachedShield
    }
}
